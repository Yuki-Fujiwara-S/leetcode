// Write an efficient algorithm that searches for a value target in an m x n integer matrix matrix. This matrix has the following properties:

// Integers in each row are sorted from left to right.
// The first integer of each row is greater than the last integer of the previous row.


/**
 * @param {number[][]} matrix
 * @param {number} target
 * @return {boolean}
 */
 var searchMatrix = function(matrix, target) {
  // we want to find largest number less than target on the first element in subarray
  // we want to find smallest number greater than target on the last element in subarray
  
  // want to check start and end of subarrays once we find range
  
  
};