// return index at which increasing - > decreasing and vice verca, return -1 if continues the same

const findIndex = (arr) => {
  let increasing;
  
  if (arr[0] < arr[1]) {
    increasing = true;
  } else {
    increasing = false;
  }

}